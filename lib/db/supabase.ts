import type { Store } from "./store";
/** STUB — real implementation lands with the DB work package. */
export function createSupabaseStore(): Store {
  throw new Error("supabase store not implemented");
}
